apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: banking-prod
spec:
  selector:
    app: account-service
  type: ClusterIP    # Internal only; exposed via Ingress
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http

